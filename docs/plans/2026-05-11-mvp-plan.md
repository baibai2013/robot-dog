# 仿生机器狗公司系统 MVP 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭建完整的「一人公司」系统：公司基础设施 + robot-dog 项目 + 三栏看板 + Mattermost 指令驱动 Claude Agent 工作的完整闭环。

**Architecture:** 五个独立子系统顺序交付：目录重组 → Docker 基础设施 → Agent Worker → 三栏看板 → n8n 串联工作流。每个子系统完成后可独立验证。

**Tech Stack:** Python 3.12, FastAPI, Anthropic SDK, Docker Compose, Mattermost, Gitea, n8n, PostgreSQL, HTML/CSS/JS (无框架)

---

## 子系统拆分

| # | 子系统 | 产出 | 依赖 |
|---|---|---|---|
| 1 | 目录重组 | `company/` + `projects/robot-dog/` 结构 | 无 |
| 2 | Docker 基础设施 | 一条命令启动所有服务 | 子系统 1 |
| 3 | Agent Worker | Claude API 员工响应指令 | 子系统 1 |
| 4 | 三栏看板 | HTML 实时状态界面 | 子系统 1 |
| 5 | n8n 工作流 | Mattermost → Agent → #状态推送 | 子系统 2+3 |

---

# 子系统 1：目录重组

## 文件结构

```
~/work/
├── company/
│   ├── CLAUDE.md              # 公司全局上下文
│   ├── .env.example           # 环境变量模板
│   ├── employees/             # 9名员工定义
│   │   ├── management/
│   │   │   ├── product-manager.md
│   │   │   ├── project-manager.md
│   │   │   └── tech-lead.md
│   │   └── engineering/
│   │       ├── mechanical.md
│   │       ├── hardware.md
│   │       ├── firmware.md
│   │       ├── algorithm.md
│   │       ├── testing.md
│   │       └── cost.md
│   ├── infra/
│   │   ├── docker-compose.yml
│   │   ├── postgres/
│   │   │   └── init.sh
│   │   └── Dockerfile.agent
│   └── system/
│       ├── state.yaml         # 公司级状态（员工在线、活跃项目）
│       └── dashboard.py       # 三栏看板服务
└── projects/
    └── robot-dog/
        ├── CLAUDE.md          # 项目上下文
        ├── charter.md
        ├── roadmap/
        │   └── roadmap.yaml
        ├── state.yaml         # 项目任务状态
        ├── domains/           # 5个工程域
        │   ├── mechanical/
        │   ├── electronics/
        │   ├── firmware/
        │   ├── simulation/
        │   └── integration/
        ├── agents/
        │   ├── requirements.txt
        │   └── worker.py      # Claude API webhook server
        └── reports/
            └── log.md
```

---

### Task 1：创建 company/ 目录结构

**Files:**
- Create: `~/work/company/CLAUDE.md`
- Create: `~/work/company/employees/management/product-manager.md`
- Create: `~/work/company/employees/management/project-manager.md`
- Create: `~/work/company/employees/management/tech-lead.md`
- Create: `~/work/company/employees/engineering/mechanical.md`
- Create: `~/work/company/employees/engineering/hardware.md`
- Create: `~/work/company/employees/engineering/firmware.md`
- Create: `~/work/company/employees/engineering/algorithm.md`
- Create: `~/work/company/employees/engineering/testing.md`
- Create: `~/work/company/employees/engineering/cost.md`

- [ ] **Step 1: 创建目录**

```bash
mkdir -p ~/work/company/employees/management
mkdir -p ~/work/company/employees/engineering
mkdir -p ~/work/company/infra/postgres
mkdir -p ~/work/company/system
mkdir -p ~/work/projects
```

- [ ] **Step 2: 写 company/CLAUDE.md**

```markdown
# 仿生机器人公司 — 全局上下文

## 公司使命
设计并制造仿生机器人。首个产品：四足仿生机器狗。

## 组织架构
9名AI员工，分管理层和工程层。
- 管理层：产品经理、项目经理、技术负责人
- 工程层：机械、硬件、固件、算法、测试、成本工程师

## 工作规范
- 每个员工的职责定义在 employees/ 目录
- 项目在 ~/work/projects/ 目录下独立存放
- 所有输出物提交到对应项目的 Gitea 仓库
- 任务完成后推送消息到 Mattermost #状态频道

## 人工介入点（Gate）
标记为 gate: true 的任务完成后必须推送到 #待审批频道等待 CEO 审批。
```

- [ ] **Step 3: 写管理层员工定义**

`~/work/company/employees/management/product-manager.md`:
```markdown
# 产品经理（Product Manager）

## 角色
定义产品需求和规格，确保工程方向符合产品目标。

## 职责
- 分析用户需求，输出产品需求文档（PRD）
- 定义功能规格和验收标准
- 协调工程层理解产品意图

## 工作方式
- 收到任务后输出结构化 Markdown 文档
- 文档保存到项目的 domains/pm/specs/ 目录
- 完成后在 Mattermost #状态频道发送完成卡片

## 输出格式
每次输出包含：背景、目标、规格列表、验收标准、风险点
```

`~/work/company/employees/management/project-manager.md`:
```markdown
# 项目经理（Project Manager）

## 角色
推进项目里程碑，协调各工程师按时完成任务。

## 职责
- 维护项目 roadmap 和 state.yaml
- 检查任务依赖关系，解锁可执行任务
- 跨部门协调，发现并上报阻塞点

## 工作方式
- 每次被触发时读取 projects/<project>/state.yaml
- 找到所有 ready 状态的任务，按优先级排列
- 向对应工程师发起任务指派（通过 n8n webhook）
- 更新任务状态并推送到 #状态频道

## 输出格式
状态更新卡片：当前进度、进行中任务、下一步行动
```

`~/work/company/employees/management/tech-lead.md`:
```markdown
# 技术负责人（Tech Lead）

## 角色
把关技术决策，确保各工程域方案一致可行。

## 职责
- 审查工程师的技术方案，提出改进建议
- 跨域技术协调（如机械尺寸影响PCB布局时）
- 识别技术风险，提出备选方案

## 工作方式
- 收到技术方案文档后进行审查
- 输出审查意见：通过 / 需修改 / 拒绝 + 原因
- 重大技术决策推送到 #待审批频道等待 CEO 确认
```

- [ ] **Step 4: 写工程层员工定义**

`~/work/company/employees/engineering/mechanical.md`:
```markdown
# 机械工程师（Mechanical Engineer）

## 角色
负责机器人结构设计、CAD建模、样机规格定义。

## 职责
- 使用 build123d 进行参数化 CAD 建模
- 输出零件尺寸规格、材料清单、公差要求
- 与硬件工程师对接安装位置和空间约束

## 工作方式
- 输出保存到 projects/<project>/domains/mechanical/output/<task-id>/
- 规格文档保存到 projects/<project>/domains/mechanical/specs/
- 完成后提交到 Gitea，开 PR，推送到 #状态频道

## 工具
- build123d (CAD)
- 导出格式：STEP, STL, DXF

## 思维方式
像机械师一样思考：每个尺寸有原因，每个公差有依据。
```

`~/work/company/employees/engineering/hardware.md`:
```markdown
# 硬件工程师（Hardware Engineer）

## 角色
负责电子系统设计：原理图、PCB规划、元器件选型。

## 职责
- 设计电机驱动板、主控板原理图
- 元器件选型，优先选 LCSC 有货型号
- 输出接口规格供固件工程师参考

## 工作方式
- 原理图描述保存到 domains/electronics/output/<task-id>/
- 接口规格保存到 domains/electronics/specs/
- BOM 输出为 CSV 格式

## 输出格式
每次输出包含：设计说明、原理图描述（Markdown）、BOM.csv、接口定义
```

`~/work/company/employees/engineering/firmware.md`:
```markdown
# 固件工程师（Firmware Engineer）

## 角色
负责嵌入式固件开发：通信协议、电机控制、传感器驱动。

## 职责
- 定义主控与驱动板通信协议
- 实现 PID/FOC 电机控制框架
- 编写传感器（IMU、编码器）驱动代码

## 工作方式
- 代码保存到 domains/firmware/output/<task-id>/src/
- 协议文档保存到 domains/firmware/specs/
- 包含单元测试

## 语言
Python（仿真/原型）、C/C++（目标嵌入式平台）
```

`~/work/company/employees/engineering/algorithm.md`:
```markdown
# 算法工程师（Algorithm Engineer）

## 角色
负责运动控制算法：步态规划、FK/IK、仿真验证。

## 职责
- 实现正向/逆向运动学（FK/IK）
- PyBullet 步态仿真
- 输出 URDF 机器人描述文件

## 工作方式
- 仿真脚本保存到 domains/simulation/output/<task-id>/
- 所有脚本支持 headless 和 GUI 两种模式运行
- 输出步态参数：步频、步幅、支撑相比例
```

`~/work/company/employees/engineering/testing.md`:
```markdown
# 测试工程师（Test Engineer）

## 角色
负责系统集成验证和跨域一致性检查。

## 职责
- 验证机械 BOM 与电子 BOM 的一致性
- 验证 URDF 几何与 CAD 尺寸一致
- 验证固件接口与硬件原理图一致

## 工作方式
- 输出一致性检查报告到 domains/integration/output/<task-id>/
- 发现不一致时创建 Gitea Issue 指派给相关工程师
- Gate 节点：所有检查通过才能进入下一里程碑
```

`~/work/company/employees/engineering/cost.md`:
```markdown
# 成本工程师（Cost Engineer）

## 角色
负责整机成本核算、BOM 比价、预算控制。

## 职责
- 汇总所有工程师的 BOM，计算整机物料成本
- 在 LCSC、立创商城、淘宝查询实时价格（描述查询方式）
- 识别高成本零件，提出替代方案

## 工作方式
- 成本报告保存到 domains/cost/output/<task-id>/cost-report.md
- BOM 汇总表为 CSV 格式，包含：料号、描述、数量、单价、总价、供应商
- 当整机成本超出预算 10% 时推送警告到 #待审批频道

## 输出格式
cost-report.md + bom-summary.csv
```

- [ ] **Step 5: 验证目录结构**

```bash
find ~/work/company -type f | sort
```

期望输出：10个 .md 文件全部存在。

- [ ] **Step 6: git init 并提交**

```bash
cd ~/work/company
git init
git add .
git commit -m "feat: company OS initial structure — 9 employee definitions"
```

---

### Task 2：迁移 robot-dog 项目到 projects/

**Files:**
- Create: `~/work/projects/robot-dog/` (从现有 robot-dog/ 迁移)
- Modify: `~/work/projects/robot-dog/CLAUDE.md`
- Create: `~/work/projects/robot-dog/state.yaml`

- [ ] **Step 1: 创建 projects/ 并迁移**

```bash
mkdir -p ~/work/projects
# 复制现有项目（保留 git 历史）
cp -r ~/work/robot-dog ~/work/projects/robot-dog
# 删除旧项目中的 infra/（移到 company/）
rm -rf ~/work/projects/robot-dog/infra
# 删除旧项目中的 agents/requirements.txt（移到 company/）
rm -rf ~/work/projects/robot-dog/agents
```

- [ ] **Step 2: 更新项目 CLAUDE.md**

`~/work/projects/robot-dog/CLAUDE.md`:
```markdown
# Robot Dog 项目 — 项目上下文

## 项目目标
设计并制造仿生四足机器狗。

## 与公司系统的关系
- 员工定义在 ~/work/company/employees/
- 基础设施在 ~/work/company/infra/
- 本目录只包含项目相关内容

## 目录结构
- charter.md — 项目宪章（约束、目标、成功标准）
- roadmap/roadmap.yaml — 里程碑和任务依赖
- state.yaml — 任务状态（orchestrator 维护）
- domains/ — 各工程域的输入规格和输出产物
- reports/log.md — 任务完成日志

## 里程碑
M0→M5，见 roadmap/roadmap.yaml

## 成本约束
见 charter.md（填写后生效）
```

- [ ] **Step 3: 迁移 infra/ 到 company/**

```bash
cp -r ~/work/robot-dog/infra ~/work/company/
cp ~/work/robot-dog/agents/requirements.txt ~/work/company/
```

- [ ] **Step 4: 验证两个项目结构完整**

```bash
echo "=== company/ ===" && find ~/work/company -not -path '*/.git/*' -type f | sort
echo "=== projects/robot-dog/ ===" && find ~/work/projects/robot-dog -not -path '*/.git/*' -type f | sort
```

- [ ] **Step 5: 提交 projects/robot-dog**

```bash
cd ~/work/projects/robot-dog
git add .
git commit -m "refactor: migrate to projects/robot-dog — remove infra (moved to company/)"
```

---

# 子系统 2：Docker 基础设施

## 文件结构

```
~/work/company/infra/
├── docker-compose.yml      # 所有服务定义
├── .env.example            # 环境变量模板
├── .env                    # 实际配置（gitignore）
├── postgres/
│   └── init.sh             # 创建多个数据库
└── Dockerfile.agent        # Agent worker 镜像
```

---

### Task 3：docker-compose.yml

**Files:**
- Create/Modify: `~/work/company/infra/docker-compose.yml`
- Create: `~/work/company/infra/.env`

- [ ] **Step 1: 写 .env**

```bash
cat > ~/work/company/infra/.env << 'EOF'
POSTGRES_PASSWORD=robotdog_dev_2026
N8N_PASSWORD=n8n_admin_2026
ANTHROPIC_API_KEY=           # 填入你的 key
GITEA_USER=admin
GITEA_TOKEN=                 # setup 后填入
MATTERMOST_TOKEN=            # setup 后填入
EOF
```

- [ ] **Step 2: 写 docker-compose.yml**

`~/work/company/infra/docker-compose.yml`:
```yaml
services:

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgres/init.sh:/docker-entrypoint-initdb.d/init.sh
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin"]
      interval: 10s
      timeout: 5s
      retries: 5

  gitea:
    image: gitea/gitea:1.22
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "3000:3000"
      - "222:22"
    environment:
      - GITEA__database__DB_TYPE=postgres
      - GITEA__database__HOST=postgres:5432
      - GITEA__database__NAME=gitea
      - GITEA__database__USER=admin
      - GITEA__database__PASSWD=${POSTGRES_PASSWORD}
      - GITEA__server__DOMAIN=localhost
      - GITEA__server__ROOT_URL=http://localhost:3000
      - GITEA__webhook__ALLOWED_HOST_LIST=*
      - GITEA__security__INSTALL_LOCK=true
    volumes:
      - gitea_data:/data

  mattermost:
    image: mattermost/mattermost-team-edition:9.11
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "8065:8065"
    environment:
      - MM_SQLSETTINGS_DRIVERNAME=postgres
      - MM_SQLSETTINGS_DATASOURCE=postgres://admin:${POSTGRES_PASSWORD}@postgres:5432/mattermost?sslmode=disable
      - MM_SERVICESETTINGS_SITEURL=http://localhost:8065
      - MM_SERVICESETTINGS_ENABLEBOTACCOUNTCREATION=true
      - MM_SERVICESETTINGS_ENABLEINCOMINGWEBHOOKS=true
      - MM_SERVICESETTINGS_ENABLEOUTGOINGWEBHOOKS=true
      - MM_SERVICESETTINGS_ENABLESLASHCOMMANDS=true
    volumes:
      - mattermost_data:/mattermost/data

  n8n:
    image: docker.n8n.io/n8nio/n8n:1.56.1
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=admin
      - N8N_BASIC_AUTH_PASSWORD=${N8N_PASSWORD}
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_HOST=postgres
      - DB_POSTGRESDB_DATABASE=n8n
      - DB_POSTGRESDB_USER=admin
      - DB_POSTGRESDB_PASSWORD=${POSTGRES_PASSWORD}
      - WEBHOOK_URL=http://localhost:5678
      - GENERIC_TIMEZONE=Asia/Shanghai
    volumes:
      - n8n_data:/home/node/.n8n

volumes:
  postgres_data:
  gitea_data:
  mattermost_data:
  n8n_data:

networks:
  default:
    name: company-net
```

- [ ] **Step 3: 写 postgres/init.sh**

```bash
cat > ~/work/company/infra/postgres/init.sh << 'EOF'
#!/bin/bash
set -e
psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" <<-EOSQL
    CREATE DATABASE gitea;
    CREATE DATABASE mattermost;
    CREATE DATABASE n8n;
EOSQL
EOF
chmod +x ~/work/company/infra/postgres/init.sh
```

- [ ] **Step 4: 启动并验证**

```bash
cd ~/work/company/infra
docker compose up -d postgres
sleep 15
docker compose ps
```

期望：postgres 状态为 `healthy`。

- [ ] **Step 5: 启动全部服务**

```bash
docker compose up -d
sleep 30
docker compose ps
```

期望：4个服务全部 `running`。检查：
- Gitea: `curl -s http://localhost:3000 | grep -i gitea`
- Mattermost: `curl -s http://localhost:8065 | grep -i mattermost`
- n8n: `curl -s -u admin:n8n_admin_2026 http://localhost:5678/healthz`

- [ ] **Step 6: 提交**

```bash
cd ~/work/company
echo ".env" >> .gitignore
git add infra/ .gitignore
git commit -m "feat: docker-compose — postgres + gitea + mattermost + n8n"
```

---

# 子系统 3：Agent Worker

## 文件结构

```
~/work/company/
└── agents/
    ├── requirements.txt
    ├── base.py         # Claude API 基础调用封装
    ├── worker.py       # FastAPI webhook server
    └── employees/      # 9个员工的 prompt 函数
        ├── __init__.py
        ├── mechanical.py
        ├── hardware.py
        ├── firmware.py
        ├── algorithm.py
        ├── testing.py
        ├── cost.py
        ├── product_manager.py
        ├── project_manager.py
        └── tech_lead.py
```

---

### Task 4：base.py — Claude API 封装

**Files:**
- Create: `~/work/company/agents/base.py`
- Create: `~/work/company/agents/requirements.txt`

- [ ] **Step 1: 写 requirements.txt**

```
anthropic>=0.40.0
fastapi>=0.115.0
uvicorn>=0.32.0
httpx>=0.27.0
pyyaml>=6.0
python-dotenv>=1.0.0
```

- [ ] **Step 2: 写 base.py**

`~/work/company/agents/base.py`:
```python
"""Claude API base — shared by all employee agents."""
import os
from pathlib import Path
from typing import Any

import anthropic
import httpx
from dotenv import load_dotenv

load_dotenv(Path(__file__).parent.parent / "infra" / ".env")

_client = anthropic.Anthropic(api_key=os.environ["ANTHROPIC_API_KEY"])

MATTERMOST_URL = os.getenv("MATTERMOST_URL", "http://localhost:8065")
MATTERMOST_TOKEN = os.getenv("MATTERMOST_TOKEN", "")


def call_claude(system: str, user: str, max_tokens: int = 4096) -> str:
    """Call Claude API and return text response."""
    msg = _client.messages.create(
        model="claude-sonnet-4-6",
        max_tokens=max_tokens,
        system=system,
        messages=[{"role": "user", "content": user}],
    )
    return msg.content[0].text


def post_to_mattermost(channel_id: str, text: str, props: dict[str, Any] | None = None) -> bool:
    """Post a message to a Mattermost channel."""
    if not MATTERMOST_TOKEN:
        print(f"[MATTERMOST] (no token) → {channel_id}: {text[:80]}")
        return False
    payload: dict[str, Any] = {"channel_id": channel_id, "message": text}
    if props:
        payload["props"] = props
    resp = httpx.post(
        f"{MATTERMOST_URL}/api/v4/posts",
        json=payload,
        headers={"Authorization": f"Bearer {MATTERMOST_TOKEN}"},
        timeout=10,
    )
    return resp.status_code == 201


def format_completion_card(task_id: str, employee: str, summary: str, output_path: str) -> str:
    """Format a task completion message card for #状态频道."""
    return (
        f"### ✅ 任务完成\n"
        f"**员工:** {employee}  \n"
        f"**任务:** `{task_id}`  \n"
        f"**摘要:** {summary}  \n"
        f"**输出:** `{output_path}`"
    )


def format_gate_card(task_id: str, task_name: str, description: str, action: str) -> str:
    """Format a gate approval request card for #待审批频道."""
    return (
        f"### ⚠️ 需要你的决策\n"
        f"**任务:** `{task_id}` — {task_name}  \n"
        f"**说明:** {description}  \n"
        f"**操作:** {action}"
    )
```

- [ ] **Step 3: 安装依赖并验证 import**

```bash
cd ~/work/company
python -m venv .venv
source .venv/bin/activate
pip install -r agents/requirements.txt
python -c "from agents.base import call_claude; print('OK')"
```

期望输出：`OK`

---

### Task 5：员工 prompt 函数

**Files:**
- Create: `~/work/company/agents/employees/__init__.py`
- Create: `~/work/company/agents/employees/mechanical.py`
- Create: `~/work/company/agents/employees/project_manager.py`（其余类似）

- [ ] **Step 1: 写 __init__.py**

```python
# employees/__init__.py
from .mechanical import run as mechanical
from .hardware import run as hardware
from .firmware import run as firmware
from .algorithm import run as algorithm
from .testing import run as testing
from .cost import run as cost
from .product_manager import run as product_manager
from .project_manager import run as project_manager
from .tech_lead import run as tech_lead

REGISTRY: dict[str, callable] = {
    "mechanical": mechanical,
    "hardware": hardware,
    "firmware": firmware,
    "algorithm": algorithm,
    "testing": testing,
    "cost": cost,
    "product_manager": product_manager,
    "project_manager": project_manager,
    "tech_lead": tech_lead,
}
```

- [ ] **Step 2: 写 mechanical.py**

`~/work/company/agents/employees/mechanical.py`:
```python
"""机械工程师 Agent"""
from pathlib import Path
from agents.base import call_claude

SYSTEM = """你是一名机械工程师，专注于仿生机器人结构设计。

职责：
- 使用 build123d 进行参数化 CAD 建模（输出 Python 代码）
- 制定零件尺寸规格和公差要求
- 与硬件工程师对接安装约束

工作规范：
- 所有尺寸单位为 mm
- 每个尺寸都要有工程依据，不硬编码无意义的数字
- 输出物包含：设计说明.md + build123d代码.py + 规格表

思维方式：像机械师一样思考，每个设计决策都要能向同事解释原因。
"""


def run(task: str, context: str = "", project_root: str = "") -> dict:
    """
    执行机械工程任务。
    返回 {"summary": str, "output": str, "content": str}
    """
    prompt = f"项目上下文：\n{context}\n\n任务：\n{task}" if context else f"任务：\n{task}"
    content = call_claude(SYSTEM, prompt)

    # 保存输出
    if project_root:
        out_dir = Path(project_root) / "domains" / "mechanical" / "output" / "latest"
        out_dir.mkdir(parents=True, exist_ok=True)
        (out_dir / "output.md").write_text(content)

    # 提取第一行作为摘要
    summary = content.strip().split("\n")[0][:120]
    return {
        "summary": summary,
        "output": str(Path(project_root) / "domains/mechanical/output/latest") if project_root else "",
        "content": content,
    }
```

- [ ] **Step 3: 写 project_manager.py**

`~/work/company/agents/employees/project_manager.py`:
```python
"""项目经理 Agent"""
from pathlib import Path
import yaml
from agents.base import call_claude

SYSTEM = """你是项目经理，负责推进项目里程碑和协调各工程师。

职责：
- 读取 state.yaml 找到可执行任务
- 输出当前项目状态报告
- 识别阻塞点并提出解决方案

输出格式（严格按此格式）：
## 项目状态报告
**总体进度：** X/Y 任务完成
**当前进行中：** [列表]
**待解锁任务：** [列表]
**阻塞点：** [列表或"无"]
**建议下一步：** [具体行动]
"""


def run(task: str, context: str = "", project_root: str = "") -> dict:
    # 读取 state.yaml 作为上下文
    state_info = ""
    if project_root:
        state_file = Path(project_root) / "state.yaml"
        if state_file.exists():
            state_info = state_file.read_text()

    prompt = f"state.yaml 内容：\n```yaml\n{state_info}\n```\n\n任务：{task}"
    content = call_claude(SYSTEM, prompt)
    summary = content.strip().split("\n")[0][:120]
    return {"summary": summary, "output": "", "content": content}
```

- [ ] **Step 4: 写其余 7 个员工（同结构，不同 SYSTEM prompt）**

为每个员工创建对应文件，SYSTEM prompt 参考 `~/work/company/employees/engineering/` 目录中的 .md 文件内容。文件列表：
- `hardware.py` — 硬件工程师
- `firmware.py` — 固件工程师
- `algorithm.py` — 算法工程师
- `testing.py` — 测试工程师
- `cost.py` — 成本工程师
- `product_manager.py` — 已完成
- `tech_lead.py` — 技术负责人

每个文件结构与 `mechanical.py` 完全相同，只替换 SYSTEM 变量内容。

- [ ] **Step 5: 验证 registry 可以导入所有员工**

```bash
cd ~/work/company
source .venv/bin/activate
python -c "from agents.employees import REGISTRY; print(list(REGISTRY.keys()))"
```

期望输出：
```
['mechanical', 'hardware', 'firmware', 'algorithm', 'testing', 'cost', 'product_manager', 'project_manager', 'tech_lead']
```

- [ ] **Step 6: 提交**

```bash
cd ~/work/company
git add agents/
git commit -m "feat: agent worker — 9 employee Claude API functions"
```

---

### Task 6：worker.py — FastAPI Webhook Server

**Files:**
- Create: `~/work/company/agents/worker.py`

- [ ] **Step 1: 写 worker.py**

`~/work/company/agents/worker.py`:
```python
"""
Agent Worker — FastAPI webhook server
n8n 调用此服务来执行员工任务

POST /run
  body: {"employee": "mechanical", "task": "...", "context": "...", "project_root": "..."}
  response: {"summary": "...", "output": "...", "content": "..."}

GET /health
  response: {"status": "ok", "employees": [...]}
"""
import os
from pathlib import Path

import uvicorn
from dotenv import load_dotenv
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel

load_dotenv(Path(__file__).parent.parent / "infra" / ".env")

from agents.employees import REGISTRY  # noqa: E402
from agents.base import post_to_mattermost, format_completion_card  # noqa: E402

app = FastAPI(title="Agent Worker", version="1.0")

# Channel IDs — 从 Mattermost 获取后填入 .env
STATUS_CHANNEL = os.getenv("MM_STATUS_CHANNEL_ID", "")
APPROVAL_CHANNEL = os.getenv("MM_APPROVAL_CHANNEL_ID", "")


class RunRequest(BaseModel):
    employee: str
    task: str
    task_id: str = "unknown"
    context: str = ""
    project_root: str = ""
    is_gate: bool = False


class RunResponse(BaseModel):
    summary: str
    output: str
    content: str


@app.get("/health")
def health():
    return {"status": "ok", "employees": list(REGISTRY.keys())}


@app.post("/run", response_model=RunResponse)
def run_employee(req: RunRequest):
    if req.employee not in REGISTRY:
        raise HTTPException(status_code=400, detail=f"Unknown employee: {req.employee}. Valid: {list(REGISTRY.keys())}")

    fn = REGISTRY[req.employee]
    result = fn(task=req.task, context=req.context, project_root=req.project_root)

    # 推送到 #状态频道
    if STATUS_CHANNEL:
        card = format_completion_card(
            task_id=req.task_id,
            employee=req.employee,
            summary=result["summary"],
            output_path=result["output"],
        )
        post_to_mattermost(STATUS_CHANNEL, card)

    # 如果是 gate，额外推送到 #待审批频道
    if req.is_gate and APPROVAL_CHANNEL:
        from agents.base import format_gate_card
        gate_msg = format_gate_card(
            task_id=req.task_id,
            task_name=req.task,
            description=result["summary"],
            action="在此频道回复「approved」解锁后续任务",
        )
        post_to_mattermost(APPROVAL_CHANNEL, gate_msg)

    return RunResponse(**result)


if __name__ == "__main__":
    uvicorn.run("agents.worker:app", host="0.0.0.0", port=8080, reload=True)
```

- [ ] **Step 2: 启动 worker 并验证**

```bash
cd ~/work/company
source .venv/bin/activate
python -m agents.worker &
sleep 3
curl -s http://localhost:8080/health | python3 -m json.tool
```

期望输出：
```json
{"status": "ok", "employees": ["mechanical", "hardware", ...]}
```

- [ ] **Step 3: 测试调用机械工程师（需要真实 ANTHROPIC_API_KEY）**

```bash
curl -s -X POST http://localhost:8080/run \
  -H "Content-Type: application/json" \
  -d '{"employee":"mechanical","task":"简要介绍四足机器人腿部机构的设计选项","task_id":"test-001"}' \
  | python3 -m json.tool | head -20
```

期望：返回包含 `summary` 和 `content` 的 JSON。

- [ ] **Step 4: 提交**

```bash
pkill -f "agents.worker"  # 停止测试用的 worker
cd ~/work/company
git add agents/worker.py
git commit -m "feat: FastAPI webhook worker — POST /run dispatches to 9 employee agents"
```

---

# 子系统 4：三栏看板

## 文件结构

```
~/work/company/system/
└── dashboard.py    # 三栏 HTML server，读 projects/robot-dog/state.yaml
```

---

### Task 7：三栏 HTML 看板

**Files:**
- Create: `~/work/company/system/dashboard.py`

- [ ] **Step 1: 写 dashboard.py**

`~/work/company/system/dashboard.py`:
```python
#!/usr/bin/env python3
"""
三栏看板 server
Usage: python system/dashboard.py [--project /path/to/projects/robot-dog]
Open:  http://localhost:8888
"""
import argparse
import json
from datetime import datetime
from http.server import BaseHTTPRequestHandler, HTTPServer
from pathlib import Path

import yaml

DEFAULT_PROJECT = Path.home() / "work" / "projects" / "robot-dog"


def load_state(project_path: Path) -> dict:
    state_file = project_path / "state.yaml"
    roadmap_file = project_path / "roadmap" / "roadmap.yaml"
    log_file = project_path / "reports" / "log.md"

    with open(state_file) as f:
        state = yaml.safe_load(f)
    with open(roadmap_file) as f:
        roadmap = yaml.safe_load(f)

    tasks_state = state.get("tasks", {})
    total = len(tasks_state)
    done = sum(1 for t in tasks_state.values() if t.get("status") == "done")

    milestones = []
    for mid, m in roadmap["milestones"].items():
        tasks = []
        for t in m.get("tasks", []):
            tid = t["id"]
            s = tasks_state.get(tid, {})
            tasks.append({
                "id": tid, "name": t["name"], "domain": t.get("domain", "—"),
                "status": s.get("status", "pending"), "gate": t.get("gate", False),
                "completed": s.get("completed_at", ""),
            })
        m_done = sum(1 for t in tasks if t["status"] == "done")
        milestones.append({"id": mid, "name": m["name"], "done": m_done, "total": len(tasks), "tasks": tasks})

    log_lines = []
    if log_file.exists():
        log_lines = [l for l in log_file.read_text().splitlines() if l.startswith("[")][-15:][::-1]

    blocked = [t for m in milestones for t in m["tasks"] if t["status"] == "blocked_human"]
    active = [t for m in milestones for t in m["tasks"] if t["status"] == "in_progress"]

    return {
        "project": state.get("project", "robot-dog"),
        "total": total, "done": done,
        "pct": int(done / total * 100) if total else 0,
        "milestones": milestones, "blocked": blocked, "active": active,
        "log": log_lines, "now": datetime.now().strftime("%Y-%m-%d %H:%M:%S"),
    }


STATUS_ICON = {
    "done": ("✅", "#22c55e"),
    "in_progress": ("⚡", "#3b82f6"),
    "blocked_human": ("🔶", "#f59e0b"),
    "approved": ("✅", "#22c55e"),
    "ready": ("🟢", "#06b6d4"),
    "pending": ("⬜", "#4b5563"),
    "failed": ("❌", "#ef4444"),
}

DOMAIN_COLOR = {
    "mechanical": "#8b5cf6", "electronics": "#06b6d4", "firmware": "#f59e0b",
    "simulation": "#22c55e", "integration": "#ec4899", "pm": "#6b7280", "human": "#f97316",
}


def render(d: dict) -> str:
    def badge(status):
        icon, color = STATUS_ICON.get(status, ("?", "#6b7280"))
        return f'<span style="background:{color}22;color:{color};border:1px solid {color}44;padding:2px 8px;border-radius:12px;font-size:.75rem">{icon} {status}</span>'

    def dtag(domain):
        c = DOMAIN_COLOR.get(domain, "#6b7280")
        return f'<span style="background:{c}22;color:{c};padding:1px 7px;border-radius:4px;font-size:.72rem">{domain}</span>'

    # left sidebar channels
    channels_html = ""
    for ch, icon, color in [
        ("机械工程", "#", "#8b5cf6"), ("硬件电子", "#", "#06b6d4"),
        ("固件软件", "#", "#f59e0b"), ("算法仿真", "#", "#22c55e"), ("成本采购", "#", "#ec4899"),
    ]:
        channels_html += f'<div style="padding:5px 10px;border-radius:4px;font-size:.82rem;cursor:pointer;display:flex;align-items:center;gap:6px"><span style="color:{color}">#</span>{ch}</div>'

    system_channels = f'''
    <div style="padding:5px 10px;border-radius:4px;font-size:.82rem;cursor:pointer;display:flex;align-items:center;gap:6px;background:#1e1b4b">
      <span>📊</span><span style="color:#a5b4fc">#状态</span>
      {"" if not d["blocked"] else f'<span style="background:#6366f1;color:#fff;font-size:.7rem;padding:0 5px;border-radius:8px">{len(d["blocked"])}</span>'}
    </div>
    <div style="padding:5px 10px;border-radius:4px;font-size:.82rem;cursor:pointer;display:flex;align-items:center;gap:6px">
      <span>⚠️</span><span style="color:#f59e0b">#待审批</span>
      {"" if not d["blocked"] else f'<span style="background:#f59e0b;color:#000;font-size:.7rem;padding:0 5px;border-radius:8px">{len(d["blocked"])}</span>'}
    </div>
    <div style="padding:5px 10px;border-radius:4px;font-size:.82rem;cursor:pointer;display:flex;align-items:center;gap:6px">
      <span>📁</span><span>#文件输出</span>
    </div>'''

    # status feed (middle)
    feed_items = ""
    if d["blocked"]:
        for t in d["blocked"]:
            feed_items += f'''<div style="background:#1c1407;border:1px solid #78350f44;border-left:3px solid #f59e0b;border-radius:8px;padding:12px;margin-bottom:10px">
              <div style="display:flex;align-items:center;gap:8px;margin-bottom:6px">
                <span>🤖</span><span style="color:#f59e0b;font-size:.8rem;font-weight:600">系统 · 需要决策</span>
                <span style="background:#78350f;color:#f59e0b;font-size:.7rem;padding:1px 8px;border-radius:8px;margin-left:auto">⚠️ 等待</span>
              </div>
              <div style="font-size:.85rem;margin-bottom:4px"><strong>{t["id"]}</strong> — {t["name"]}</div>
              <div style="font-size:.78rem;color:#92400e">在 system/state.yaml 将此任务改为 status: approved 解锁后续任务</div>
            </div>'''
    if d["active"]:
        for t in d["active"]:
            feed_items += f'''<div style="background:#071c1c;border:1px solid #065f4644;border-left:3px solid #06b6d4;border-radius:8px;padding:12px;margin-bottom:10px">
              <div style="display:flex;align-items:center;gap:8px;margin-bottom:5px">
                <span style="font-size:1rem">⚡</span><span style="color:#06b6d4;font-size:.8rem;font-weight:600">{t["domain"]}</span>
              </div>
              <div style="font-size:.85rem">{t["id"]}: {t["name"]}</div>
            </div>'''
    for line in d["log"][:5]:
        feed_items += f'<div style="background:#0f1626;border:1px solid #1e293b;border-radius:6px;padding:10px;margin-bottom:8px;font-size:.78rem;color:#64748b">{line}</div>'

    if not feed_items:
        feed_items = '<div style="color:#334155;text-align:center;padding:40px">暂无消息</div>'

    # right task panel
    task_panel = f'''
    <div style="font-weight:600;font-size:.9rem;margin-bottom:12px">📋 任务列表</div>
    <div style="background:#0f1626;border-radius:6px;padding:10px;margin-bottom:12px">
      <div style="display:flex;justify-content:space-between;font-size:.78rem;color:#475569;margin-bottom:5px">
        <span>{d["project"]}</span><span style="color:#a5b4fc">{d["pct"]}%</span>
      </div>
      <div style="background:#1e293b;border-radius:4px;height:5px">
        <div style="background:linear-gradient(90deg,#6366f1,#8b5cf6);width:{d["pct"]}%;height:100%;border-radius:4px"></div>
      </div>
    </div>'''

    for m in d["milestones"]:
        all_done = m["done"] == m["total"] and m["total"] > 0
        m_color = "#22c55e" if all_done else "#6366f1"
        rows = ""
        for t in m["tasks"]:
            icon, color = STATUS_ICON.get(t["status"], ("?", "#6b7280"))
            rows += f'<div style="display:flex;align-items:center;gap:6px;padding:3px 0;border-bottom:1px solid #0f172a;font-size:.8rem"><span>{icon}</span><span style="flex:1;color:{"#64748b" if t["status"]=="done" else "#e2e8f0"}">{t["id"]}</span>{"<span style=font-size:.7rem>🔑</span>" if t["gate"] else ""}</div>'
        task_panel += f'''
        <div style="margin-bottom:10px">
          <div style="font-size:.78rem;font-weight:700;text-transform:uppercase;letter-spacing:.05em;color:{m_color};margin-bottom:5px;display:flex;justify-content:space-between">
            <span>{m["id"]} {m["name"].split("·")[0]}</span><span style="color:#334155">{m["done"]}/{m["total"]}</span>
          </div>
          {rows}
        </div>'''

    return f"""<!DOCTYPE html>
<html lang="zh"><head>
<meta charset="UTF-8"><meta http-equiv="refresh" content="8">
<title>{d["project"]} — Robot Dog Co.</title>
<style>
*{{box-sizing:border-box;margin:0;padding:0}}
body{{font-family:-apple-system,"SF Pro Display",sans-serif;background:#080b14;color:#e2e8f0;height:100vh;display:flex;flex-direction:column;overflow:hidden}}
.topbar{{height:44px;background:#060910;border-bottom:1px solid #1e293b;display:flex;align-items:center;padding:0 16px;gap:12px;flex-shrink:0}}
.main{{flex:1;display:flex;overflow:hidden}}
.sidebar{{width:200px;background:#060910;border-right:1px solid #1e293b;padding:12px 8px;overflow-y:auto;flex-shrink:0;display:flex;flex-direction:column;gap:2px}}
.feed{{flex:1;display:flex;flex-direction:column;overflow:hidden}}
.feed-header{{padding:10px 16px;border-bottom:1px solid #1e293b;font-weight:600;font-size:.88rem;flex-shrink:0;background:#0a0d14}}
.feed-messages{{flex:1;padding:12px 16px;overflow-y:auto}}
.feed-input{{padding:10px 16px;border-top:1px solid #1e293b;background:#0a0d14;flex-shrink:0}}
.feed-input-box{{background:#1e293b;border-radius:6px;padding:9px 12px;font-size:.82rem;color:#475569}}
.taskpanel{{width:220px;background:#060910;border-left:1px solid #1e293b;padding:12px;overflow-y:auto;flex-shrink:0}}
.sec-label{{color:#475569;font-size:.72rem;font-weight:700;text-transform:uppercase;letter-spacing:.08em;padding:8px 10px 4px}}
</style></head>
<body>
<div class="topbar">
  <span style="font-size:1.1rem;font-weight:700;color:#a5b4fc">🤖 Robot Dog Co.</span>
  <span style="font-size:.75rem;color:#334155">|</span>
  <span style="font-size:.75rem;color:#475569">{d["now"]}</span>
  <span style="margin-left:auto;font-size:.72rem;color:#22c55e">● 自动刷新</span>
</div>
<div class="main">
  <div class="sidebar">
    <div class="sec-label">项目频道</div>
    {channels_html}
    <div class="sec-label" style="margin-top:6px">系统频道</div>
    {system_channels}
    <div class="sec-label" style="margin-top:6px">员工</div>
    <div style="padding:4px 10px;font-size:.8rem;display:flex;align-items:center;gap:5px"><span style="color:#22c55e;font-size:.6rem">●</span>产品经理</div>
    <div style="padding:4px 10px;font-size:.8rem;display:flex;align-items:center;gap:5px"><span style="color:#22c55e;font-size:.6rem">●</span>项目经理</div>
    <div style="padding:4px 10px;font-size:.8rem;display:flex;align-items:center;gap:5px"><span style="color:#22c55e;font-size:.6rem">●</span>机械工程师</div>
    <div style="padding:4px 10px;font-size:.8rem;display:flex;align-items:center;gap:5px"><span style="color:#4b5563;font-size:.6rem">●</span><span style="color:#4b5563">固件工程师</span></div>
    <div style="padding:4px 10px;font-size:.8rem;display:flex;align-items:center;gap:5px"><span style="color:#4b5563;font-size:.6rem">●</span><span style="color:#4b5563">硬件工程师</span></div>
  </div>
  <div class="feed">
    <div class="feed-header">📊 #状态频道</div>
    <div class="feed-messages">{feed_items}</div>
    <div class="feed-input">
      <div class="feed-input-box">@员工名 输入指令...</div>
    </div>
  </div>
  <div class="taskpanel">{task_panel}</div>
</div>
</body></html>"""


class Handler(BaseHTTPRequestHandler):
    project_path: Path = DEFAULT_PROJECT

    def log_message(self, *_): pass

    def do_GET(self):
        if self.path == "/api/state":
            data = load_state(self.project_path)
            body = json.dumps(data, ensure_ascii=False, default=str).encode()
            self.send_response(200)
            self.send_header("Content-Type", "application/json; charset=utf-8")
            self.send_header("Content-Length", len(body))
            self.end_headers()
            self.wfile.write(body)
        else:
            try:
                html = render(load_state(self.project_path)).encode()
            except Exception as e:
                html = f"<pre style='color:red'>Error: {e}</pre>".encode()
            self.send_response(200)
            self.send_header("Content-Type", "text/html; charset=utf-8")
            self.send_header("Content-Length", len(html))
            self.end_headers()
            self.wfile.write(html)


if __name__ == "__main__":
    parser = argparse.ArgumentParser()
    parser.add_argument("--project", default=str(DEFAULT_PROJECT))
    parser.add_argument("--port", type=int, default=8888)
    args = parser.parse_args()
    Handler.project_path = Path(args.project)
    server = HTTPServer(("0.0.0.0", args.port), Handler)
    print(f"✅ Dashboard → http://localhost:{args.port}")
    print(f"   Project   → {args.project}")
    try:
        server.serve_forever()
    except KeyboardInterrupt:
        pass
```

- [ ] **Step 2: 验证看板启动**

```bash
cd ~/work/company
source .venv/bin/activate
python system/dashboard.py --project ~/work/projects/robot-dog &
sleep 2
curl -s http://localhost:8888/api/state | python3 -m json.tool | grep pct
```

期望：返回 `"pct": 0`（尚未完成任务）

- [ ] **Step 3: 打开浏览器确认三栏布局**

```bash
open http://localhost:8888
```

验证：左侧有频道列表、中间有 #状态、右侧有任务面板。

- [ ] **Step 4: 提交**

```bash
pkill -f "dashboard.py"
cd ~/work/company
git add system/dashboard.py
git commit -m "feat: three-column dashboard — sidebar + status feed + task panel"
```

---

# 子系统 5：n8n 工作流配置

> **说明:** n8n 工作流通过 UI 配置，无法纯代码交付。本任务提供完整配置步骤。

### Task 8：Mattermost → Agent 工作流

- [ ] **Step 1: Mattermost 创建 Bot 账号**

1. 打开 http://localhost:8065，用管理员账号登录
2. 进入 **System Console → Integrations → Bot Accounts**
3. 创建 Bot：用户名 `robot-pm`，描述 `Project Manager Bot`
4. 复制 Bot Token，填入 `~/work/company/infra/.env` 的 `MATTERMOST_TOKEN`

- [ ] **Step 2: Mattermost 创建必要频道**

在 Mattermost 创建以下频道（记录每个频道的 Channel ID）：
- `状态` （系统状态推送）
- `待审批` （Gate 任务通知）
- `文件输出` （产出物归档）
- `机械工程`、`硬件电子`、`固件软件`、`算法仿真`、`成本采购`

获取 Channel ID 方法：在频道页面右上角 → View Info → 复制 Channel ID。
将 `#状态` 和 `#待审批` 的 Channel ID 填入 `.env`：
```
MM_STATUS_CHANNEL_ID=<id>
MM_APPROVAL_CHANNEL_ID=<id>
```

- [ ] **Step 3: 创建 Mattermost Outgoing Webhook**

1. 进入 **Integrations → Outgoing Webhooks → Add Outgoing Webhook**
2. 配置：
   - Channel: 任意（或指定 #机械工程 等）
   - Trigger Words: `@mechanical`, `@hardware`, `@firmware`, `@algorithm`, `@testing`, `@cost`, `@pm`
   - Callback URL: `http://localhost:5678/webhook/mattermost`
3. 保存，复制 Token

- [ ] **Step 4: 在 n8n 创建 Mattermost → Agent 工作流**

1. 打开 http://localhost:5678，登录（admin / n8n_admin_2026）
2. 新建 Workflow，命名 `Mattermost → Agent`
3. 添加节点：
   - **Webhook** (Trigger): Path = `mattermost`, Method = POST
   - **Function**: 解析触发词和任务内容
     ```javascript
     const body = $input.first().json;
     const text = body.text || "";
     const employeeMap = {
       "@mechanical": "mechanical",
       "@hardware": "hardware",
       "@firmware": "firmware",
       "@algorithm": "algorithm",
       "@testing": "testing",
       "@cost": "cost",
       "@pm": "project_manager",
     };
     let employee = null;
     for (const [trigger, name] of Object.entries(employeeMap)) {
       if (text.toLowerCase().startsWith(trigger)) {
         employee = name;
         break;
       }
     }
     const task = employee ? text.replace(/^@\w+\s*/, "") : text;
     return [{ json: { employee, task, raw: body } }];
     ```
   - **IF**: 条件 `employee` 不为空
   - **HTTP Request**: POST `http://host.docker.internal:8080/run`
     - Body: `{"employee": "{{$json.employee}}", "task": "{{$json.task}}", "task_id": "mattermost-{{$now}}", "project_root": "/workspace/projects/robot-dog"}`
   - **Respond to Webhook**: 返回 `{"status": "dispatched"}`

4. 激活 Workflow

- [ ] **Step 5: 端到端测试**

在 Mattermost #机械工程 频道发送：
```
@mechanical 列出四足机器人腿部机构的三种主流设计方案，各自的优缺点是什么
```

验证：
1. n8n 收到 webhook（查看 n8n execution log）
2. Agent worker 被调用（查看 worker 日志）
3. 结果推送到 Mattermost #状态频道（有完成卡片出现）

- [ ] **Step 6: 提交最终状态**

```bash
cd ~/work/company
git add .
git commit -m "feat: MVP complete — company OS + 9 agents + dashboard + n8n workflow"

cd ~/work/projects/robot-dog
git add .
git commit -m "refactor: project structure aligned with company OS"
```

---

# 验收清单

- [ ] `docker compose up -d` 启动 4 个服务，全部 healthy
- [ ] http://localhost:3000 — Gitea 可访问
- [ ] http://localhost:8065 — Mattermost 可访问
- [ ] http://localhost:5678 — n8n 可访问
- [ ] http://localhost:8888 — 三栏看板可访问，显示任务列表
- [ ] Mattermost 发 `@mechanical 介绍腿部机构设计` → #状态频道出现完成卡片
- [ ] state.yaml 有 `blocked_human` 任务时，看板左侧频道出现红色数字
- [ ] `~/work/company/` 和 `~/work/projects/robot-dog/` 结构正确
